{
  "name": "news-bots",
  "version": "1.1.0",
  "private": true,
  "description": "A basic example of using the Slack Bolt ⚡️ SDK",
  "main": "dist/app.js",
  "scripts": {
    "build": "tsc -p .",
    "build:watch": "tsc -w -p .",
    "start": "npm run build && node dist/app.js"
  },
  "engines": {
    "node": "14.x"
  },
  "dependencies": {
    "@slack/bolt": "^3.3.0",
    "@types/node": "^14.14.35",
    "dotenv": "10.0.0",
    "newsapi": "^2.4.1",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.3"
  }
}
